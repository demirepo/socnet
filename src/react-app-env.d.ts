/// <reference types="react-scripts" />
// import appReducer from "./appReducer";
// type initialState = { isInitialized: boolean };
// type appReducer = (state: initialState, action: action) => initialState;
// type action = { type: string; action: any };
// type dispatch = () => void;
